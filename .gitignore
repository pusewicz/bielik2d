.DS_Store
.build/
.swiftpm/
Package.resolved
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# editor / tooling metadata
.serena/
.claude/

# vendored install prefix produced by scripts/build-vendor.sh
vendor/.install/
vendor/**/build/

# rebuildable shader bytecode (kept in repo for now; uncomment if we ever switch)
# Sources/Bielik2D/Shaders/*.spv
vendor/.install
